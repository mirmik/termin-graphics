# Third-party distributions bundled into the Termin SDK.
# Keep every entry exact: the build stage materializes a compatible wheel for
# every entry, then SDK population installs
# this file offline from build/python-runtime/external-wheels with --no-deps.
# Keep transitive dependencies explicit as well: the offline install deliberately
# disables dependency resolution.
colorama==0.4.6
exceptiongroup==1.3.1
glfw==2.10.0
iniconfig==2.3.0
numpy==2.5.1
packaging==26.2
pluggy==1.6.0
Pygments==2.20.0
pytest==9.1.1
PyYAML==6.0.3
tomli==2.4.1
typing_extensions==4.16.0
watchdog==6.0.0
